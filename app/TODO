Release 1.1
===========

todo:

- cr: Device configuration through multicast protocol
- ?: Support scan mode in web uit
- pr: animated gif
- cr: support for touch-screen
- cr: add extra discovery protocol
- pr: only show relevant items in the web-gui

done:

2009-09-29 cr: kleine font kleiner
- cr: sleep mode (except for switching off lcd backlight + scanner LED's)

Can not / Will not do
=====================

- ?: Switching off (LCD backlight + scanner LED's)

+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Release 1.0
===========

done:

- Support external HR100 scanner. Hans: fix bootup with HID connected
- Watchdog support in kernel. Hans: fix watchdog driver
- support configuration save/restore over FTP
- Support internal CCD scanner
- Bytecode compile lua before installing
- reboot progress bar IE
- DHCP vendor ID
- Query scanner version and display in web interface
- GPIO
- aanpassen welkomtstekst
- Testen discovery over subnets
- WPA2
- Remove leftmost dead pixel from screen
- Timeout on info screen
- Reduce bootup time configuring scanner (trigger mode?)
- Factory defaults in reboot screen
- Font rendering
- Client mode debugging
- Reboot na upgrade
- Wifi disable for non-wifi units
- show_config screen timeout
- Testen wifi
- Serial number set via barcode
- Start discovery after netwerk up
- RFS version number
- LED's aansturen
- Fix config barcodes, broken with latest code-id update

can't reproduce:

- 'foute' barcodes
